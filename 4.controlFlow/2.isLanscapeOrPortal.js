function isLanscape(width, height) {
  //   return width > height ? true : false; this is amature code because we dont need to the ? question mark before postion. just right below things
  return width > height;
}
console.log(isLanscape(1940, 1400));
