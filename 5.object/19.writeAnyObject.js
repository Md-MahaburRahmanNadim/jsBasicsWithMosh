// scall object
const scall = {
  height: 2,
  width: 30,
  magermant() {
    console.log("it measure the length of any object in centmeter or inch");
  },
};
console.log(scall.width);
console.log(scall.height);
scall.magermant();

// pen object
const pen = {
  color: "has a varity of color",
  types: ["bolpen", "gelpen", "signpen"],
  uses() {
    console.log("to write in paper we use pen");
  },
};
